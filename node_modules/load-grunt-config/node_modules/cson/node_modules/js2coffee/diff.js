(function() {
  var ansidiff, file, fs, input, inputContent, node, output, outputContent, _ref;

  ansidiff = require('ansidiff');

  fs = require('fs');

  _ref = process.argv, node = _ref[0], file = _ref[1], input = _ref[2], output = _ref[3];

  inputContent = fs.readFileSync(input);

  outputContent = fs.readFileSync(output);

  console.log(ansidiff.lines(outputContent, inputContent));

}).call(this);
